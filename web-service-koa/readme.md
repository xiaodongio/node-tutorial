```
npm init -y
```

```
npm i --save koa koa-router koa-bodyparser koa-compress koa-session --registry=https://registry.npm.taobao.org

npm i --save-dev @types/koa-compress @types/koa-bodyparser @types/koa-session --registry=https://registry.npm.taobao.org
```


